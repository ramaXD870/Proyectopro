// Configuración de las ofertas (aquí pones tus datos reales)
interface PokemonCard {
    id: number;
    title: string;
    edition: string;
    image: string; // URL de tu imagen
    originalPrice: number;
    currentPrice: number;
    discount: number;
}
